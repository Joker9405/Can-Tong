# CanTongMVP 部署与自检（简）

1. 将 `data/seed.csv` 放在可被静态访问的路径（本仓库即 `/data/seed.csv`）。  
2. 部署到 Vercel 后，浏览器直接打开：`/data/seed.csv` 应能看到内容。  
3. 主页搜索 “不要推 / don't push / 唔好擁 / 咪擁我” 应有结果。  
4. 未命中时页面会提示“未找到，欢迎扩展”。
